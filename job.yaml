apiVersion: batch/v1
kind: Job
metadata:
  name: loan-approval-job
spec:
  template:
    spec:
      containers:
      - name: loan-approval
        image: loan-approval-app:latest
        imagePullPolicy: Never
      restartPolicy: Never
  backoffLimit: 2
